[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn 'backend.server:create_app()' --bind 0.0.0.0:8080 --workers 1 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"

